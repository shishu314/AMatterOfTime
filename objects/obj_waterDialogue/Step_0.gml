/// @description Insert description here
// You can write your code in this editor
if(!hasPrompted && obj_player.x >= x) {
	InitializeDialogBox("Damn, global warming has really taken its toll");
	hasPrompted = true;
}